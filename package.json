{
  "name": "test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "gulp": "^4.0.0"
  },
  "scripts": {
    "build": "npm run build:automation && npm run lint",
    "build:automation": "cd automation && npm run build",
    "lint": "gulp lint --gulpfile automation/lib/gulpfile.js"
  },
  "devDependencies": {
    "@types/gulp": "^4.0.5"
  }
}
